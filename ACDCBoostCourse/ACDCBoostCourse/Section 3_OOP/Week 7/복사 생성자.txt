2. 복사 생성자

 /*(두들 낙서 67강 참조)
 char * strData; // strData는 char를 가리킴
 void* ptr = strData; // 그 주소값을 void*에 저장할 수 있음
 *ptr = 'A'; // 따라서 이 구문은 에러
 (void*)strData라고 하는 이유는 strData가 문자열로 인식될 가능성이 존재해서*/

        복사 생성자 
	- 얕은 복사 : 같은 메모리를 할당
	- 깊은 복사 : 새로운 메모리를 할당